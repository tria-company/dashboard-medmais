/**
 * Cookie name used for simulated authentication.
 * Used by middleware and login page.
 */
export const AUTH_COOKIE_NAME = "auth-token";

/**
 * Simulated token value for prototype (no backend).
 * Replace with real token when API is available.
 */
export const AUTH_COOKIE_VALUE = "authenticated";
