// Mantido apenas por compatibilidade histórica. Os filtros globais agora
// são renderizados por GlobalFiltersBar e controlados via FiltersContext.
export default function FiltroPostoEmpresa(): null {
  return null;
}
